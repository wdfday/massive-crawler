# === Required ===
# Polygon API keys (comma-separated). One or more keys; more keys → more workers / throughput.
POLYGON_API_KEYS=your_key1,your_key2

# === Data directories ===
# Local data directory. Default: data (Polygon data under data/Polygon/).
# DATA_DIR=data
# Host path for data when running via Docker. Default: ./data
# DATA_DIR_HOST=./data

# === Output format / profile ===
# SAVE_FORMAT: parquet | csv | json.
# If SAVE_FORMAT is not set, it is derived from PROFILE (dev→json, prod→parquet).
SAVE_FORMAT=parquet
PROFILE=prod

# === Stock selection ===
# Currently only "file" is used; tickers are always loaded from a file/indices list.
STOCK_SELECTION=file
# When STOCK_SELECTION=file: path to ticker list.
# Empty or unset falls back to indices/combined.txt (or other indices/*.txt/.json).
# TICKERS_FILE=indices/combined.txt

# === Schedule ===
# Next run time (UTC): hour 0-23, minute 0-59. Default 00:30 UTC
# PHASE2_RUN_HOUR=0
# PHASE2_RUN_MINUTE=30

# === Logging ===
# Log level: debug | info | warn | error. Default: info
LOG_LEVEL=info
