# Data provider: polygon | tiingo
DATA_PROVIDER=polygon

# Lưu packet: csv | parquet | json. Nếu không set, mặc định theo PROFILE (dev→json, prod→parquet)
SAVE_FORMAT=parquet

# Profile: dev (mặc định SAVE_FORMAT=json) | prod (mặc định SAVE_FORMAT=parquet). Chỉ dùng khi không set SAVE_FORMAT
PROFILE=prod

# Thư mục gốc lưu dữ liệu (mặc định: data). Polygon: data/Polygon/{Ticker}/
DATA_DIR=data

# Polygon API key(s), phân cách bằng dấu phẩy nếu nhiều key
POLYGON_API_KEY=
# POLYGON_API_KEYS=key1,key2

# Cách chọn key: round-robin | least-used
KEY_STRATEGY=round-robin

# Cách lấy danh sách ticker: file | top-marketcap | top-volume | sp500 | any
STOCK_SELECTION=file
# Đường dẫn file ticker (khi STOCK_SELECTION=file)
# TICKERS_FILE=indices/combined.txt
